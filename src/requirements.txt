1. When user changes timer type, past timer is resetted
2. In settings user can change minutes of all timers
3. When user changes minutes of started timer, the timer is resetted
4. User can start the timer using TimersPlan UI
5. If user stops or resets the timer or changes TimerType when TimersPlan is active, the current PlanItem is stopped
6. User can reset PlanItem only using PlanItem UI
7. 